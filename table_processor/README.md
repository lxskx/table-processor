# Table Processor 🤡💥

Очередной велосипед для работы с таблицами, потому что pandas — это скучно.

## Чо умеет эта поделка:

- **Читать/писать CSV** - как Excel, только криво
- **Pickle** - сохраняем всё, даже если не надо
- **Текстовые файлы** - для истинных ценителей ASCII-арта
- **Арифметика** - складываем, вычитаем, умножаем... иногда даже правильно!
- **Фильтрация** - находим строки по принципу "нравится/не нравится"

## Как юзать (на свой страх и риск):

```python
from table_processor import Table, load_csv

# Создаем таблицу с данными (или без - кому как повезет)
data = [
    [1, "Вася", 25, 50000],
    [2, "Петя", 30, 60000], 
    [3, "Коля", 35, 70000]
]
headers = ["id", "имя", "возраст", "зарплата"]
table = Table(data, headers)

# Смотрим что наваяли
table.print_table()
# Вывод:
# id  имя   возраст  зарплата  
# ----------------------------
# 1   Вася  25       50000     
# 2   Петя  30       60000     
# 3   Коля  35       70000     

# Сохраняем в CSV (файл может получиться, а может и нет)
save_csv(table, "база_данных.csv")

# Фильтруем богатых
rich_guys = table.filter_rows(table.gr(55000, "зарплата"))
print("Богачи:")
rich_guys.print_table()
Установка (если осмелились):

bash
# Клонируем этот шедевр
git clone https://github.com/ТВОЙ_НИК/table-processor.git

# Заходим в папку (можно не заходить)
cd table-processor

# Запускаем тесты (или не запускаем - там страшно)
python test.py
Особенности реализации:

🐛 Баги - это не баги, это фичи
🎲 Рандом - иногда работает, иногда нет
📝 Документация - "и так сойдет"
🚀 Производительность - главное чтобы работало
Когда использовать:

Когда pandas слишком стабилен и предсказуем
Хочется почувствовать себя настоящим программистом
Нужно впечатлить преподавателя "оригинальным подходом"
Просто скучно
Когда НЕ использовать:

В продакшене
Для важных данных
Если цените свое время
Вообще всегда
Пример вывода ошибки (для настроения):

text
Traceback (most recent call last):
  File "test.py", line 42, in <module>
    table.add(5000, "зарплата")
AttributeError: 'Table' object has no attribute 'add'
# Сюрприз! 😅
Статус проекта:

Код написан
Тесты падают
Profit 💰
P.S. Не говорите, что я вас не предупреждал 🎭
